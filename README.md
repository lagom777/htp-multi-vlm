# HTP Multi-VLM Framework

학습 데이터 없이(zero-shot) HTP(House-Tree-Person) 심리검사 그림의 객체를 식별하는 **Multi-VLM 투표 + 이미지 재검증 프레임워크**입니다. 석사 학위논문 「HTP 심리검사 그림 분석을 위한 Multi-VLM 프레임워크」(서강대학교 AI·SW대학원, 2026)의 실험 코드와 측정 결과를 담고 있는 공개 저장소입니다.

## 프레임워크 개요

그림 한 장당 모델 호출 4회로 동작합니다.

1. **Round 1 — 독립 분석 (3회)**: 서로 다른 제공사의 VLM 3개가 동일한 그림을 각각 분석. 각 모델에는 정답 항목·준정답 항목·교란 항목을 혼합한 카테고리별 50항목 체크리스트가 프롬프트로 제공됨 (정답 목록 노출 차단)
2. **다수결 투표 (2/3)**: 2개 이상 모델이 보고한 객체는 합의 통과, 1개 모델만 보고한 객체는 보류
3. **이미지 재검증 (1회)**: 검증 모델이 보류 객체의 경계 상자를 단서로 원본 이미지를 다시 확인하여 채택/기각
4. **최종 확정**: 합의 객체 + 재검증 통과 객체, 9-구역(3×3) 위치 산출(단일 모델 우선 bbox)

## 주요 결과 (64장, AI Hub 266)

| 환경 | 구성 모델 | F1 | 재현율 | 정밀도 | 최저 F1 | 9-구역 위치 |
|---|---|---|---|---|---|---|
| 고사양 로컬 (33B급, 4bit) | Qwen3.6-35B-A3B · EXAONE-4.5-33B · Gemma4-26B-A4B | **94.6** | 92.1 | 97.2 | 64.0 | 91.9 |
| 저사양 로컬 (2~4B급) | Qwen3VL-4B · InternVL3.5-2B · gemma-3-4b | **79.6** | 91.6 | 70.3 | 42.6 | 92.3 |
| 상용 API | Gemini 3.1 Pro · GPT-5.5 · Claude Sonnet 4.6 | **96.8** | 97.4 | 96.3 | 85.7 | 96.4 |

- 프레임워크 효과(최고 성능 단독 모델 대비): 저사양 로컬 **+3.0%p** · 고사양 로컬 **+3.0%p** · 상용 API +0.1%p — 단독 모델이 약하고 불안정한 로컬 환경일수록 이득이 큼
- 그림별 최저 F1은 세 환경 모두에서 어느 단독 모델보다 높음 (worst-case 개선)
- 채점: 정답 항목은 AI Hub 정답 라벨과 대조하고, 정답 라벨에 없는 보고는 원본 그림과 직접 대조하여 실제로 그려졌는지 판정

## 저장소 구성

```
framework/    핵심 파이프라인
  a_aspect.py              모델 호출·GT 로드·채점 유틸 (데이터 경로는 아래 실행 방법 참고)
  v6_voting_debate_4.py    투표 + 이미지 재검증 코어 (OpenRouter 클라이언트)
  test64_v5.py             64장 본실험 실행 스크립트
  test32_v5_robust.py      32장 예비 실행 스크립트
  plans_v5.py              카테고리별 50항목 체크리스트 (정답·준정답·교란)
  eval_9region.py          9-구역 위치 평가
  test_64_files.py         평가 표본 64장 목록
  project_paths.py         데이터·결과·설정 경로 관리
  common_utils.py / g_prime.py / en_to_kr.py / clean_prompts_v2.py  유틸
experiments/  설계 탐색·구성요소 비교 실험 (투표 임계값, 토의(debate), SAM2/탐지-먼저, 타일 분할 등)
results/      측정 결과 JSON (그림별 R1 응답·투표·재검증·채점 전체 기록)
  test64_v5_robust.json    고사양 로컬 64장
  test64_v5_lowspec.json   저사양 로컬 64장
  test64_api_v5.json       상용 API 64장
  test64_v5_textjudge.json 텍스트 판정 vs 이미지 재검증 통제 비교
  v6_ablation_results.json 토의 단계 비교 실험
  plans_v5_data.json       체크리스트 데이터
  reference_v60.json       최종 확정 지표 재계산 값
outputs/      새 실행 결과가 저장되는 폴더(실행 시 자동 생성, Git 제외)
```

## 실행 방법

1. **데이터**: [AI Hub 266 「AI 기반 아동 미술심리 진단을 위한 그림 데이터 구축」](https://aihub.or.kr/)에서 데이터셋을 직접 내려받은 뒤, 저장소 루트의 `data/` 아래에 배치하세요 (`data/01.원천데이터/TS_집/...`, `data/02.라벨링데이터/TL_집/...`). 다른 위치를 쓰려면 환경변수 `HTP266_IMG_DIR`·`HTP266_LBL_DIR`로 지정할 수 있습니다. **데이터셋의 이미지·라벨은 AI Hub 이용약관에 따라 이 저장소에 포함되어 있지 않습니다(재배포 불가).**
2. **API 키**: `config.example.json`을 `config.json`으로 복사한 뒤 OpenRouter 키를 입력하거나, 환경변수 `OPENROUTER_API_KEY`를 설정하세요. 일부 비교 실험은 같은 파일의 Google·OpenAI·Grok 키를 추가로 사용합니다. `config.json`은 Git에서 제외됩니다. 로컬 모델은 vLLM 서빙 엔드포인트를 사용합니다.
3. **실행** (저장소 루트에서):
   ```bash
   pip install -r requirements.txt
   python framework/test64_v5.py
   ```
   새 결과 JSON·로그는 기본적으로 `outputs/`에 생성됩니다. 저장 위치는 환경변수 `HTP_OUTPUT_DIR`로 바꿀 수 있습니다.

## 인용

논문은 현재 심사 중이며, 확정 후 서지 정보를 추가할 예정입니다.

## 라이선스

별도의 라이선스가 지정되지 않은 공개 저장소입니다. 코드와 결과는 논문 심사·연구 참고 목적으로 열람할 수 있으며, 재사용·재배포가 필요하면 저자에게 문의해 주세요. 라이선스는 논문 확정 후 지정할 예정입니다.
